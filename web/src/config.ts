export const config = {
    // Set these two if the setting above IS_BACKEND_HTTPS is set to true
    BACKEND_PORT: 4000,
    IS_BACKEND_HTTPS: false,

    BACKEND_DOMAIN_IS_THE_SAME_AS_THIS_APP: true,

    // Set it if the setting above is set to false
    BACKEND_URL: 'https://example.com/graphql'
}