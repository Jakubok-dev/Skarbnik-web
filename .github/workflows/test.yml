name: test

on:
  push:
  pull_request:

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v2
        with:
          node-version: '16.x'
      - run: npm --prefix ./teapot ci
      - run: npm run --prefix ./teapot testLinux

      - run: npm --prefix ./magnifier ci
      - run: npm run --prefix ./magnifier build